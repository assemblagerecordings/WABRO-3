# AI Council Summary

## Rule

Each AI role can read the whole WABRO folder but writes only to its assigned file.

The summary file records merged decisions and next actions.

## Current Decision

Layer 1 comes first:

```text
Backwards V5 + Muse 2/simulator + 8 macros + snare geometry + persistent state
```

## Next Action

Test the hub in simulator mode, confirm `snare_geometry` appears in `/state`, then map OSC values to TouchDesigner and Bitwig.

